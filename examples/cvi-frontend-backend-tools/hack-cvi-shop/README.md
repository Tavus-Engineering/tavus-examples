to run the project, you need to have vite installed.

```bash
npm install
```

then, you can run the project with the following command:

```bash
npm run dev
```

Also run local node js server local-node folder